@model ECommerceApp.Models.ProfileViewModel

<div class="container mt-5">
    <div class="row">
        <!-- Sidebar Hồ sơ -->
        <div class="col-md-3">
            <div class="card">
                <div class="card-body text-center">
                    <!-- Ảnh đại diện -->
                    <img src="@Model.ImgUrl" class="img-fluid rounded-circle mb-3" alt="Ảnh đại diện" width="150" height="150">
                    <h4>@Model.UserFullName</h4>
                    <p class="text-muted">Tên đăng nhập: @User.Identity.Name</p>
                </div>
            </div>
            <div class="card mt-3">
                <div class="card-body">
                    <h5 class="card-title">Thông tin hồ sơ</h5>
                    <p><strong>Email:</strong> @Model.Email</p>
                    <p><strong>Số điện thoại:</strong> @Model.PhoneNumber</p>
                    <p><strong>Ngày đăng ký:</strong> <!-- Thêm ngày đăng ký tại đây --> </p>
                </div>
            </div>
        </div>

        <!-- Nội dung chính của hồ sơ -->
        <div class="col-md-9">
            <div class="card">
                <div class="card-header">
                    <h5>Cập nhật hồ sơ</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Profile" method="post" enctype="multipart/form-data">
                        <div class="form-group">
                            <label for="UserFullName">Tên đầy đủ</label>
                            <input type="text" class="form-control" id="UserFullName" name="UserFullName" value="@Model.UserFullName" />
                        </div>
                        <div class="form-group">
                            <label for="Email">Email</label>
                            <input type="email" class="form-control" id="Email" name="Email" value="@Model.Email" />
                        </div>
                        <div class="form-group">
                            <label for="PhoneNumber">Số điện thoại</label>
                            <input type="text" class="form-control" id="PhoneNumber" name="PhoneNumber" value="@Model.PhoneNumber" />
                        </div>
                        <div class="form-group">
                            <label for="ProfileImageFile">Ảnh đại diện mới</label>
                            <input type="file" class="form-control-file" id="ProfileImageFile" name="ProfileImageFile" />
                            <small class="form-text text-muted">Ảnh đại diện hiện tại: @Model.ImgUrl</small>
                        </div>

                        <!-- Phần thay đổi mật khẩu -->
                        <div class="form-group mt-4">
                            <button type="button" class="btn btn-secondary" id="changePasswordBtn">Thay đổi mật khẩu</button>
                        </div>
                        <div id="passwordFields" style="display: none;">
                            <div class="form-group">
                                <label for="Password">Mật khẩu mới</label>
                                <input type="password" class="form-control" id="Password" name="Password" />
                            </div>
                            <div class="form-group">
                                <label for="ConfirmPassword">Xác nhận mật khẩu mới</label>
                                <input type="password" class="form-control" id="ConfirmPassword" name="ConfirmPassword" />
                            </div>
                        </div>

                        <button type="submit" class="btn btn-primary">Cập nhật</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.getElementById('changePasswordBtn').addEventListener('click', function () {
        var passwordFields = document.getElementById('passwordFields');
        if (passwordFields.style.display === 'none') {
            passwordFields.style.display = 'block';
        } else {
            passwordFields.style.display = 'none';
        }
    });
</script>
